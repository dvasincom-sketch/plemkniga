node_modules
.next
.env
.env.local
build
dist
*.tsbuildinfo
next-env.d.ts
/media
.DS_Store
_to_delete/

# Исходные наборы внешних данных: качаются по DOI, в репозиторий не кладутся.
# Хранится не сам набор, а посчитанные по нему кривые с версией и ссылкой —
# см. docs/vneshnie-dannye.md.
/data
