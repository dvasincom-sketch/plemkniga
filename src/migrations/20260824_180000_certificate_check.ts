import { MigrateUpArgs, MigrateDownArgs, sql } from '@payloadcms/db-postgres'

/**
 * Код проверки подлинности на выданном документе (ТЗ, UC-03).
 *
 * Держатель бумаги должен уметь проверить её подлинность, не входя
 * в систему. Одного номера для этого мало: номера идут подряд, и открытая
 * проверка по номеру превратилась бы в способ выгрузить из книги, какие
 * животные каким хозяйствам принадлежат, в обход и публичности записей,
 * и точечного доступа. Код печатается на бланке рядом с QR: номер называет
 * документ, код доказывает, что документ у вас в руках.
 *
 * ## Почему у прежних документов код тоже появляется
 *
 * Оставить их без кода — значит либо пускать по одному номеру (то есть
 * открыть тот самый перебор), либо не проверять вовсе. Код им проставляется,
 * но на выданных бланках он не напечатан: страница проверки честно говорит
 * такому держателю, что документ выпущен до появления проверки, и отправляет
 * в Ассоциацию.
 *
 * ## Почему `gen_random_uuid`, а не `random()`
 *
 * `random()` в PostgreSQL — не криптографический источник, и при заполнении
 * одним запросом даёт значения, восстановимые по нескольким известным.
 * Здесь это означало бы: получив пару настоящих кодов, можно вычислить
 * остальные. `gen_random_uuid()` берёт случайность у операционной системы.
 * Модуль pgcrypto для него не нужен начиная с PostgreSQL 13. `md5` рядом
 * ничего не защищает, а только перемешивает готовые случайные байты
 * в удобную для нарезки строку.
 *
 * ## Почему в выражении участвует `d.id`
 *
 * Не ради уникальности — она уже есть в случайности. Первая редакция
 * собирала код в несвязанном подзапросе, и PostgreSQL, совершенно
 * законно, выполнил его **один раз на весь UPDATE**: все пятьсот
 * документов получили один и тот же код. Ошибка тихая — коды выглядят
 * настоящими, длина верная, — и обнаружилась только запуском на живой
 * базе. Ссылка на колонку обновляемой строки делает подзапрос связанным,
 * и он выполняется для каждой строки.
 *
 * Алфавит тот же, что в коде выпуска (`certificate-check.ts`): без `0`/`O`,
 * `1`/`I`/`L`, `5`/`S`, `8`/`B` — эти пары неразличимы на плохой печати,
 * а код переписывают глазами.
 */
export async function up({ db }: MigrateUpArgs): Promise<void> {
  await db.execute(sql`
  ALTER TABLE "documents" ADD COLUMN IF NOT EXISTS "public_code" varchar;`)

  /*
   * Заполняется только то, что действительно выдано Ассоциацией.
   * Загруженные хозяйством бумаги (справка, договор) документами книги
   * не являются, и проверять их подлинность через книгу нечем.
   */
  await db.execute(sql`
  UPDATE "documents" d
     SET "public_code" = (
       SELECT string_agg(
         substr('234679ACDEFGHJKMNPQRTUVWXYZ',
                (get_byte(decode(md5(gen_random_uuid()::text || d."id"::text), 'hex'), g) % 27) + 1, 1),
         '' ORDER BY g)
         FROM generate_series(0, 7) AS g
     )
   WHERE d."issued_by_id" IS NOT NULL
     AND d."public_code" IS NULL;`)

  await db.execute(sql`
  CREATE INDEX IF NOT EXISTS "documents_public_code_idx" ON "documents" USING btree ("public_code");`)
}

export async function down({ db }: MigrateDownArgs): Promise<void> {
  await db.execute(sql`
  DROP INDEX IF EXISTS "documents_public_code_idx";
  ALTER TABLE "documents" DROP COLUMN IF EXISTS "public_code";`)
}
