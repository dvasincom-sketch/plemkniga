/**
 * Сколько записей можно подать одной заявкой на верификацию.
 *
 * Ограничение не техническое. Заявку разбирает человек, и «подал всё стадо
 * целиком» означает не быструю верификацию, а очередь, которая не двинется.
 * Двести записей — примерно день работы эксперта; больше следует делить
 * на части, и лучше это сделает хозяйство, чем Ассоциация.
 *
 * Живёт отдельным файлом, потому что число нужно и серверному действию,
 * и форме в браузере: файл с директивой `use server` не вправе экспортировать
 * ничего, кроме асинхронных функций.
 */
export const VERIFICATION_LIMIT = 200
