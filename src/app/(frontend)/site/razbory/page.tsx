import { redirect } from 'next/navigation'

/**
 * Адрес без языка.
 *
 * Раздел живёт по-русски, но ссылку на него дают короткой — из письма,
 * из разговора, из чужой публикации. Пусть работает: перенаправление
 * постоянное и ведёт на русскую версию, единственную существующую.
 */
export default function RedirectNotes() {
  redirect('/ru/razbory')
}
