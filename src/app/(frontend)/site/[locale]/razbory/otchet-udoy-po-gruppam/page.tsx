import type { Metadata } from 'next'
import Link from 'next/link'
import { notFound } from 'next/navigation'
import { ProductFooter, ProductHeader } from '@/components/site/ProductShell'
import { NoteHeader, NoteNeighbours, NoteSources } from '@/components/site/NoteFrame'
import { TermsSeen } from '@/components/site/TermsSeen'
import { NOTES, noteBySlug } from '@/lib/notes'
import { pageMetadata } from '@/lib/seo'
import { isLocale, type Locale } from '@/lib/i18n/locales'
import { NBSP, nf, pct } from '@/lib/format'
import {
  finishedLactation,
  hasMilk305,
  lactationGroup,
  LACTATION_GROUP_LABEL,
} from '@/lib/sql-lactation'
import { HERD_THRESHOLDS, PLAUSIBLE } from '@/lib/checks-registry'
import { MIN_TESTS } from '@/lib/completeness'
import { MIN_DAUGHTERS } from '@/lib/sire-summary'

const SLUG = 'otchet-udoy-po-gruppam'

export async function generateMetadata(): Promise<Metadata> {
  const note = noteBySlug(SLUG)!
  return pageMetadata({
    title: note.title,
    description: note.lead,
    path: `/ru/razbory/${SLUG}`,
  })
}

/**
 * Разбор: удой за 305 дней по группам лактаций.
 *
 * ## Зачем написан
 *
 * Таблицу из трёх строк читают как рейтинг: первотёлки хуже, третья
 * группа лучше, значит стадо взрослеет правильно. Ни одного из этих
 * выводов из отчёта не следует. Группы поставлены рядом не для того,
 * чтобы их сравнивали между собой, — разница между ними это возраст,
 * известный заранее, — а для того, чтобы каждую можно было сравнить
 * с её же прошлым и с той же группой у соседа.
 *
 * Второе, о чём отчёт молчать не должен: среднее растёт не только
 * от продуктивности. Незаконченная лактация в него не входит, а корова,
 * выбракованная посреди лактации, свою последнюю запись в него так
 * и не отдаст — то есть строгая выбраковка поднимает среднее сама
 * по себе. Разбор поэтому кончается перечнем причин, по которым число
 * выросло, а стадо не изменилось.
 *
 * ## Почему условия показаны кусками SQL, а не пересказаны
 *
 * Утверждение «законченной считается лактация с датой окончания либо
 * с 305 дойными днями» проверяемое, и проверить его читатель должен
 * прямо здесь. Три предиката на странице собираются вызовами
 * из `lib/sql-lactation.ts` — из того же места, откуда их берёт запрос
 * отчёта, сравнение дочерей быка и список на выбраковку. Пересказанные
 * словами, они разошлись бы с запросом на первой правке правила,
 * и разошлись бы молча.
 *
 * ## Что нашлось по дороге
 *
 * Числа на одной карточке стоят в разных единицах: колонка группы
 * считает строки лактаций, а «в ходу» рядом — коров. Обе подписи верны
 * по отдельности, и обе легко сложить в уме, чего делать нельзя.
 * Сказано об этом на странице прямо.
 *
 * Второе: у этого среднего нет окна. Соседние отчёты меряют скорость
 * (выбытие за год, воспроизводство за двенадцать и восемнадцать месяцев),
 * а здесь усредняется вся история книги разом. Отсюда его инертность —
 * и отсюда же вывод, что заметный сдвиг такого числа за квартал почти
 * всегда означает изменение данных, а не стада.
 */

/* --------------------------- Правила отбора ------------------------------- */

/**
 * Три условия, из которых собран запрос. Куски SQL берутся вызовами:
 * страница показывает ровно тот текст, который уходит в базу.
 */
const RULES: { title: string; sql: string; what: string }[] = [
  {
    title: 'Лактация закончена',
    sql: finishedLactation('l'),
    what:
      'Два признака через «или», и второй не запасной: дату окончания заполняют не все, ' +
      'а дойные дни приезжают из программы доения сами.',
  },
  {
    title: 'У лактации есть удой за 305 дней',
    sql: hasMilk305('l'),
    what:
      'Без него сравнивать нечего. Ноль отсекается вместе с пустотой: «не доила» ' +
      'и «не записали» по нулю неразличимы.',
  },
  {
    title: 'Номер лактации превращается в группу',
    sql: lactationGroup('lactation'),
    what:
      'Три группы вместо пяти. Ноль попадает в первую вместе с единицей: непроставленный ' +
      'номер почти всегда первотёлка — у коровы постарше номер обычно есть.',
  },
]

/** Подписи групп — те же, что в отчёте, а не набранные заново. */
const GROUPS = [1, 2, 3].map((key) => ({ key, label: LACTATION_GROUP_LABEL[key]! }))

/** Границы правдоподобия удоя строкой: «500…25 000 кг». */
const MILK_RANGE =
  `${nf(PLAUSIBLE.milkYield.min, 0)}…${nf(PLAUSIBLE.milkYield.max, 0)}${NBSP}кг`

export default async function MilkByLactationNotePage({
  params,
}: {
  params: Promise<{ locale: string }>
}) {
  const { locale: raw } = await params
  if (!isLocale(raw)) notFound()
  const locale: Locale = raw
  const note = noteBySlug(SLUG)!

  return (
    <>
      <ProductHeader locale={locale} />

      <main className="container-page pb-16">
        <NoteHeader note={note} />

        {/* ------------------------ Что попадает в среднее -------------------- */}
        <section className="mt-14">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            Что должно совпасть, чтобы лактация попала в среднее
          </h2>

          <p className="mt-5 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Отчёт не берёт всё подряд. Строка лактации проходит три условия, и каждое
            отсекает свою часть книги. Ниже они стоят так, как уходят в базу, — собранные
            из того же места, откуда их берёт сам запрос.
          </p>

          <div className="mt-6 space-y-4">
            {RULES.map((r) => (
              <div key={r.title} className="rounded-2xl border border-ink-100 bg-white p-5 sm:p-6">
                <div className="text-[16px] font-medium">{r.title}</div>
                <div className="mt-3 overflow-x-auto">
                  <code className="whitespace-pre text-[13px] text-forest-600">{r.sql}</code>
                </div>
                <p className="mt-3 max-w-[70ch] text-[15px] leading-relaxed text-ink-500">
                  {r.what}
                </p>
              </div>
            ))}
          </div>

          <p className="mt-6 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Первое условие стоит разобрать подробнее, потому что оно единственное, которое
            работает не так, как читается. «Закончена» здесь не значит «додоила норму».
            Лактация, у которой проставлена дата окончания, считается законченной при любом
            числе дойных дней: корова заболела и её запустили на сотом дне, корову продали,
            корова пала — во всех трёх случаях дата окончания есть, а удой за 305 дней
            в строке стоит маленький. Такая запись входит в среднее наравне с полной
            и тянет его вниз.
          </p>

          <p className="mt-4 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Обратная сторона того же правила честнее: лактация в 320&nbsp;дойных дней
            заведомо не в ходу, даже если конец не проставлен, и ждать от хозяйства
            заполнения поля, которое нужно для запуска, а не для отчёта, было бы способом
            потерять данные. Ровно 305&nbsp;дней бывает и у идущей лактации — такую корову
            в среднее берут намеренно: норму она уже додоила.
          </p>
        </section>

        {/* ------------------------ Лактации, а не коровы --------------------- */}
        <section className="mt-14 max-w-[75ch]">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            В колонке стоят лактации, а не коровы
          </h2>

          <p className="mt-5 text-[16px] leading-relaxed text-ink-700">
            Это первое, обо что спотыкаются: сумма трёх строк больше числа коров в стаде.
            Так и должно быть. Единица счёта здесь — строка лактации, и у коровы
            с тремя отёлами таких строк три, по одной в каждой группе. Колонка так
            и подписана — «Лактаций», — и складывать её с поголовьем нельзя.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            У этого есть следствие внутри третьей группы, и оно неочевидное. Группа
            «{LACTATION_GROUP_LABEL[3]}» собирает третью, четвёртую, пятую лактации одной
            и той же коровы: долгожительница отдаёт в неё четыре строки, а корова, только
            что отелившаяся третьим отёлом, — одну. Среднее по группе поэтому взвешено
            долголетием: чем дольше живут ваши лучшие коровы, тем сильнее они тянут его
            на себя. Это не ошибка счёта, но читать третью строку как «сколько даёт
            взрослая корова» после этого уже нельзя.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Рядом на той же карточке стоит число в других единицах — «столько-то коров
            доят сейчас». Оно считается по животным, а не по строкам: у коровы с двумя
            незакрытыми лактациями это одна корова, а не две. Обе подписи верны
            по отдельности, и сложить их в уме легче, чем заметить разницу. Не складывайте.
          </p>
        </section>

        {/* ---------------------------- «В ходу» ------------------------------ */}
        <section className="mt-14 max-w-[75ch]">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            «В ходу» — рядом с числом, но не внутри него
          </h2>

          <p className="mt-5 text-[16px] leading-relaxed text-ink-700">
            Строка под таблицей называет коров с незакрытой лактацией и ведёт в их список.
            Незакрытая — это конец не проставлен и дойных дней меньше 305. В средние такие
            лактации не входят, и причина простая: «ещё доит» — не то же, что «мало
            надоила». Смешав их с законченными, среднее занижалось бы тем сильнее, чем
            больше в стаде недавних отёлов, то есть отчёт наказывал бы хозяйство
            за пополнение.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Тонкость, которую стоит знать: в это число попадают не все доящиеся коровы,
            а только те, у кого в незакрытой лактации уже проставлен удой за 305 дней.
            Пока поле пусто, корова не видна ни в таблице, ни в строке под ней — она доит,
            и книга об этом молчит. Число «в ходу» поэтому не отвечает на вопрос «сколько
            у меня дойных»; оно отвечает на другой: сколько лактаций дозреет до среднего
            в ближайшие месяцы.
          </p>

          <div className="mt-6 rounded-2xl bg-brand-50 px-6 py-5">
            <p className="text-[16px] leading-relaxed text-ink-700">
              Практическое правило: чем больше «в ходу» относительно строк в таблице, тем
              осторожнее со средним. Оно посчитано по прошлому стада, а заметная часть
              настоящего в нём ещё не учтена.
            </p>
          </div>
        </section>

        {/* --------------------- Группы и то, с чем их сравнивают ------------- */}
        <section className="mt-14">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            Группы сравниваются не между собой
          </h2>

          <p className="mt-5 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Первотёлка даёт примерно четыре пятых от того, что даст она же на третьей
            лактации. Это свойство породы, а не вашего кормления, и разность между первой
            и третьей строкой поэтому не оценка работы: она известна заранее и у всех
            примерно одинакова. Ставить группы рядом стоит только затем, чтобы никогда
            больше не сравнивать первотёлок со взрослыми.
          </p>

          <div className="mt-6 overflow-x-auto">
            <table className="data-table w-full min-w-[640px] text-[14px]">
              <thead>
                <tr>
                  <th className="w-[220px] text-left">Группа</th>
                  <th className="w-[130px] text-left">Номера лактаций</th>
                  <th className="text-left">С чем сравнивать</th>
                </tr>
              </thead>
              <tbody>
                {GROUPS.map((g) => (
                  <tr key={g.key}>
                    <td>{g.label}</td>
                    <td className="text-ink-500">
                      {g.key === 1 ? '0 и 1' : g.key === 2 ? '2' : '3 и старше'}
                    </td>
                    <td className="text-ink-500">
                      {g.key === 1
                        ? 'Со своими же первотёлками прошлых лет: это самая быстрая обратная связь по подбору быков и по выращиванию тёлок.'
                        : g.key === 2
                          ? 'С первотёлками позапрошлого года — теми же коровами годом раньше. Провал здесь означает раздой или здоровье, а не генетику.'
                          : 'Со своим же прошлым и с той же группой у соседа — при одинаковом методе контроля.'}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>

          <p className="mt-6 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Почему групп три, а не пять и не «каждая своя». Так делят в племенных отчётах:
            первотёлка ещё растёт и доит меньше взрослой, вторая лактация переходная,
            с третьей корова выходит на свой уровень, а дальше различия между лактациями
            меньше разброса внутри группы. Дробить сильнее значило бы получить строки,
            разница между которыми — шум.
          </p>

          <h3 className="mt-8 max-w-[75ch] text-[19px] font-medium leading-tight">
            Почему нет приведения к взрослому эквиваленту
          </h3>

          <p className="mt-3 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            В Канаде и США общий показатель приводят к взрослому эквиваленту: удой
            первотёлки умножают на коэффициент и получают «сколько бы она дала взрослой».
            Коэффициенты там опубликованы по породе, региону и сезону отёла
            и пересматриваются. Тогда стадо описывается одним числом, и это удобно.
          </p>

          <p className="mt-3 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            У нас таких таблиц нет. Взять коэффициент с потолка значило бы превратить
            измерение в оценку, а оценку — в чужую, никем не подтверждённую: удой,
            умноженный на выдуманное число, выглядит ровно так же убедительно, как удой.
            Поэтому здесь принят отечественный порядок — 305&nbsp;дней, показанные
            раздельно. Он отвечает на тот же вопрос, ничего не выдумывая: сравнивать надо
            первотёлок с первотёлками.
          </p>
        </section>

        {/* ------------------- Почему среднее растёт без стада ---------------- */}
        <section className="mt-14 max-w-[75ch]">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            Пять причин, по которым среднее выросло, а стадо не изменилось
          </h2>

          <p className="mt-5 text-[16px] leading-relaxed text-ink-700">
            Число в строке группы поднялось на триста килограммов — это хорошая новость
            ровно до того момента, пока не проверены пять других объяснений. Перечислим
            их по убыванию частоты.
          </p>

          <h3 className="mt-8 text-[19px] font-medium leading-tight">
            Первое: выбытие слабых
          </h3>

          <p className="mt-3 text-[16px] leading-relaxed text-ink-700">
            Самое важное, потому что оно выглядит как успех. Выбывшие из расчёта
            не исключаются — и это не забытое условие: законченная лактация коровы,
            проданной после неё, это надоенное молоко, и выбросив его, мы получили бы
            среднее по тем, кого не выбраковали, тем более завышенное, чем строже отбраковка.
            Но у коровы, выбывшей <em>посреди</em> лактации, последняя запись законченной
            не станет никогда — и в среднее не попадёт. Хозяйство, которое стало
            выбраковывать низкоудойных первотёлок, не дожидаясь конца лактации, увидит рост
            среднего по первой группе, не изменив ни кормления, ни подбора.
          </p>

          <h3 className="mt-8 text-[19px] font-medium leading-tight">
            Второе: у среднего нет окна
          </h3>

          <p className="mt-3 text-[16px] leading-relaxed text-ink-700">
            Соседние отчёты меряют скорость и потому ограничены сроком: выбытие считается
            за последние двенадцать месяцев, воспроизводство — за двенадцать
            и восемнадцать. Здесь окна нет: усредняется вся история книги, и лактация,
            законченная семь лет назад, весит столько же, сколько прошлогодняя. Отсюда
            инертность числа — за квартал оно почти не двигается, — и отсюда же вывод:
            если оно всё-таки двинулось заметно, скорее всего изменились данные, а не стадо.
            Чаще всего это довнесённая история или партия импорта со своими лактациями.
          </p>

          <h3 className="mt-8 text-[19px] font-medium leading-tight">
            Третье: состав первой группы
          </h3>

          <p className="mt-3 text-[16px] leading-relaxed text-ink-700">
            В первую группу попадают лактации с номером 1 и лактации, у которых номер
            не проставлен вовсе. Допущение разумное — у коровы постарше номер обычно есть,
            — но оно означает, что перенос данных из чужой программы, где номер лактации
            не выгрузился, разбавляет первую группу записями любого возраста. Проверяется
            это глазами по списку, а не по среднему.
          </p>

          <h3 className="mt-8 text-[19px] font-medium leading-tight">
            Четвёртое: сменился метод контроля
          </h3>

          <p className="mt-3 text-[16px] leading-relaxed text-ink-700">
            Удой за 305 дней не измеряют целиком: его считают по контрольным дням, а
            промежутки досчитывают. Реже контроли — больше досчитанного, и пик лактации
            может не попасть ни в один контрольный день. Хозяйство, перешедшее
            с квартального контроля на ежемесячный, получает другой ряд чисел о тех же
            коровах. Что именно меняется вместе с методом — в{' '}
            <Link
              href="/ru/razbory/metod-kontrolya"
              className="font-medium text-forest-600 underline underline-offset-4 hover:text-forest-500"
            >
              разборе метода контроля
            </Link>
            .
          </p>

          <h3 className="mt-8 text-[19px] font-medium leading-tight">
            Пятое: округление при вводе
          </h3>

          <p className="mt-3 text-[16px] leading-relaxed text-ink-700">
            Измеренный удой круглым бывает редко. Если больше{' '}
            {pct(HERD_THRESHOLDS.roundedShare)} удоев в стаде кратны 500&nbsp;кг, это оценка
            на глаз или перенос из отчёта, где числа уже округлили, — и книга говорит
            об этом замечанием. Округление вверх на полтонны у части записей поднимает
            среднее ровно настолько, насколько кажется незначительным каждое отдельное
            округление.
          </p>
        </section>

        {/* ------------------ Число записано, а не измерено ------------------- */}
        <section className="mt-14 max-w-[75ch]">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            Откуда берётся сам удой за 305 дней
          </h2>

          <p className="mt-5 text-[16px] leading-relaxed text-ink-700">
            Скажем прямо, потому что это самая важная оговорка страницы. Удой за 305 дней
            в книге — хранимое поле строки лактации. Оно приходит выгрузкой из программы
            доения, импортом файла или вводом; расчёта этого числа из ряда контрольных
            доений в книге нет. Отчёт по группам усредняет то, что записано, и ни одного
            нового измерения при этом не делает.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Контрольные доения при этом хранятся отдельной записью каждое — дата, суточный
            удой, жир, белок, соматические клетки, лаборатория, — и держат их рядом
            с лактацией не для красоты: по ряду доений видно, на скольких точках стоит
            заявленное число и есть ли в ряду разрывы. Введённое число само по себе
            проверить нечем; проверяем мы его тем, что под ним.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Требование к плотности ряда в книге есть, и оно одно: чтобы запись прошла
            проверку полноты, у животного с заявленным удоем за 305 дней должно быть
            не меньше {MIN_TESTS}&nbsp;замеров. Число взято нижней границей — при более
            редком контроле отрезок между замерами переваливает за 75&nbsp;дней, и кривая
            лактации между двумя точками восстанавливается уже не по данным,
            а по предположению. Важно, где это требование работает: при подаче записи
            на подтверждение, а не при попадании в среднее по стаду. В отчёт лактация
            с одним замером под ней входит наравне с лактацией, под которой их двенадцать.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Границы правдоподобия {MILK_RANGE} тоже не сторожат эту таблицу: они стоят
            над сводным удоем в карточке животного, а не над строкой лактации. Рамка
            вдобавок заведомо широкая — двадцать пять тысяч встречается у мировых
            рекордисток, сорок ни у кого, — и ловит она ошибку в единицах измерения,
            а не неверное число. Ближе к делу проверки по стаду: они мерят запись
            не общей границей, а самим стадом. Удой, отличающийся от медианы своего стада
            больше чем в {nf(HERD_THRESHOLDS.outlierFactor, 0)}&nbsp;раза в любую сторону,
            приходит замечанием; доли при этом не считаются вовсе, пока записей меньше{' '}
            {nf(HERD_THRESHOLDS.minHerd, 0)} — в маленьком стаде любая доля случайность.
          </p>
        </section>

        {/* ------------------------------ Пределы ----------------------------- */}
        <section className="mt-14 max-w-[75ch] rounded-2xl border border-ink-100 bg-white p-6 sm:p-8">
          <h2 className="text-[20px] font-medium leading-tight">Пределы, названные до вопроса</h2>

          <div className="mt-5 space-y-4 text-[15px] leading-relaxed text-ink-500">
            <p>
              <strong className="font-medium text-ink-700">
                Метод контроля в отчёте не показан.
              </strong>{' '}
              У каждого доения он записан, а в среднее по группам не попадает: таблица
              усредняет удои независимо от того, кто снимал показания и как часто.
              Внутри одного хозяйства это обычно неважно — порядок один на всех, — а вот
              сравнивать эту таблицу с чужой без вопроса о методе нельзя.
            </p>
            <p>
              <strong className="font-medium text-ink-700">
                Жир и белок здесь — простые средние процентов.
              </strong>{' '}
              Они не взвешены удоем, то есть корова, надоившая четыре тонны, и корова,
              надоившая двенадцать, вносят в процент поровну. Для сравнения групп между
              годами этого хватает; для расчёта килограммов жира по стаду — нет.
            </p>
            <p>
              <strong className="font-medium text-ink-700">
                Это не то же среднее, что в сравнении дочерей быка.
              </strong>{' '}
              Деление на группы там общее с этим отчётом — один и тот же кусок кода, — но
              выборка другая: там берётся по одной, последней законченной, лактации
              на живую корову. Здесь — все законченные строки, включая лактации выбывших.
              Числа поэтому разойдутся, и вычитать одно из другого нельзя. Разница
              со сверстницами к тому же не считается, пока у быка меньше{' '}
              {MIN_DAUGHTERS}&nbsp;дочерей: по двум это совпадение, а не результат.
            </p>
            <p>
              <strong className="font-medium text-ink-700">Стада как отбора здесь нет.</strong>{' '}
              В расчёт идут все самки хозяйства, кроме архивных, — без деления по фермам
              и группам содержания. Хозяйство с двумя площадками увидит одно число на обе,
              и разницу между площадками эта таблица не покажет.
            </p>
          </div>
        </section>

        {/* ------------------------ Чего это не доказывает -------------------- */}
        <section className="mt-14 max-w-[75ch]">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            Чего этот разбор не доказывает
          </h2>

          <div className="mt-5 space-y-4 text-[16px] leading-relaxed text-ink-700">
            <p>
              <strong className="font-medium">
                Что третья группа показывает лучшую генетику, чем первая.
              </strong>{' '}
              Разница между ними — возраст, и она примерно одинакова у всех стад. Хуже
              того, третья группа состоит из коров, доживших до третьей лактации, то есть
              из уже отобранных: сравнивая её с первой, вы сравниваете отобранных
              с неотобранными и приписываете разницу возрасту дважды.
            </p>
            <p>
              <strong className="font-medium">Что рост числа — рост продуктивности.</strong>{' '}
              Он бывает им, а бывает следствием выбраковки посреди лактации, довнесённой
              истории, смены метода контроля или округления при вводе. Прежде чем
              радоваться, стоит посмотреть, изменилось ли число строк в группе и сколько
              лактаций стоит «в ходу».
            </p>
            <p>
              <strong className="font-medium">
                Что удой за 305 дней измерен книгой.
              </strong>{' '}
              Он в неё записан. Книга хранит под ним контрольные доения и показывает,
              насколько плотен их ряд, но сама число не считает и не пересчитывает.
              Расчёт лактации из доений — следующий шаг, и до него мы о нём молчим.
            </p>
            <p>
              <strong className="font-medium">
                Что эту таблицу можно поставить рядом с чужой.
              </strong>{' '}
              Не разобравшись в методе контроля — нельзя. Удой за 305 дней под официальным
              контролем и он же под хозяйским — числа разной ценности, и рейтинг, сложивший
              их вместе, вводит в заблуждение обоих участников. Это отдельный разговор,
              и он{' '}
              <Link
                href="/ru/razbory/metod-kontrolya"
                className="font-medium text-forest-600 underline underline-offset-4 hover:text-forest-500"
              >
                разобран отдельно
              </Link>
              .
            </p>
          </div>
        </section>

        <p className="mt-10 max-w-[75ch] text-[15px] leading-relaxed text-ink-500">
          Как лактации и контрольные доения устроены в самой книге — на странице{' '}
          <Link
            href={`/${locale}/book/milk`}
            className="font-medium text-forest-600 underline underline-offset-4 hover:text-forest-500"
          >
            «Лактации и контрольные доения»
          </Link>
          , вместе с названными пределами.
        </p>

        <TermsSeen slugs={note.terms} />
        <NoteSources note={note} />
        <NoteNeighbours notes={NOTES} current={SLUG} />
      </main>

      <ProductFooter lang={locale} />
    </>
  )
}
