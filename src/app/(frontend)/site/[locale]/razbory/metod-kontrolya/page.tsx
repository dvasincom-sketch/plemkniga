import type { Metadata } from 'next'
import { Fragment } from 'react'
import Link from 'next/link'
import { notFound } from 'next/navigation'
import { ProductFooter, ProductHeader } from '@/components/site/ProductShell'
import { NoteHeader, NoteNeighbours, NoteSources } from '@/components/site/NoteFrame'
import { NOTES, noteBySlug } from '@/lib/notes'
import { pageMetadata } from '@/lib/seo'
import { isLocale, type Locale } from '@/lib/i18n/locales'
import {
  RECORDING_PROTOCOL,
  RECORDING_SCHEME,
  SAMPLING_MOMENT,
  SAMPLING_SCHEME,
  recordingLabel,
} from '@/lib/milk-recording'

const SLUG = 'metod-kontrolya'

export async function generateMetadata(): Promise<Metadata> {
  const note = noteBySlug(SLUG)!
  return pageMetadata({
    title: note.title,
    description: note.lead,
    path: `/ru/razbory/${SLUG}`,
  })
}

/**
 * Разбор: метод контроля продуктивности и сопоставимость лактаций.
 *
 * ## Зачем написан
 *
 * Про метод контроля в хозяйствах либо не думают вовсе, либо думают
 * как про графу отчёта. А это единственное, что отвечает на вопрос,
 * можно ли поставить два удоя рядом. Ошибка здесь не видна никому:
 * оба числа правдоподобны, оба посчитаны верно, и только сравнение
 * между ними оказывается ни о чём. Разбор поэтому начинается не со
 * стандарта, а со случая, который узнаёт каждый: два хозяйства называют
 * одно и то же число, и это разные числа.
 *
 * ## Почему значения перечислений выведены из кода
 *
 * Страница утверждает, что метод записан значениями стандарта, а не
 * строкой. Утверждение проверяемое, и проверить его читатель должен
 * прямо здесь: таблицы собираются из `lib/milk-recording.ts` — из того
 * же места, откуда их берёт форма контрольного доения и откуда они
 * уходят в обмен. Переписанные руками, они разошлись бы с обменом
 * на первой же правке стандарта, и разошлись бы молча.
 *
 * Привычное «A4» на странице тоже не набрано буквами: его собирает
 * `recordingLabel` — та самая функция, что показывает обозначение
 * человеку в карточке.
 *
 * ## Почему практический раздел, а не одно объяснение
 *
 * Объяснить букву мало: зоотехник, дочитав, остаётся с вопросом «а у
 * меня-то какой метод». Поэтому в середине разбора стоят четыре вопроса,
 * по которым метод определяется без документов, и отдельно сказано,
 * что делать со старыми записями, — там соблазн проставить «A4» задним
 * числом сильнее всего.
 */
export default async function MilkRecordingNotePage({
  params,
}: {
  params: Promise<{ locale: string }>
}) {
  const { locale: raw } = await params
  if (!isLocale(raw)) notFound()
  const locale: Locale = raw
  const note = noteBySlug(SLUG)!

  /*
   * Два случая из первого раздела. Обозначения собираются той же
   * функцией, что и в карточке доения: если правило сборки изменится,
   * разбор изменится вместе с ней, а не останется памятником.
   */
  /*
   * Оба обозначения собирает та же функция, что показывает их в карточке
   * доения: цифра в них — интервал в неделях, посчитанный из числа
   * контролей в год. Набери мы «A4» буквами, страница и книга разошлись бы
   * ровно в том месте, о котором страница и написана.
   */
  const FIRST = recordingLabel({ protocol: 'A-OfficialMRORepresentative', perYear: 12 })
  const SECOND = recordingLabel({ protocol: 'B-HerdOwnerOrNominee', perYear: 4 })

  /*
   * Три перечисления, кроме протокола: он показан выше карточками,
   * потому что про букву спрашивают, а про остальное — нет.
   */
  const GROUPS: {
    field: string
    title: string
    what: string
    rows: [value: string, label: string, hint?: string][]
  }[] = [
    {
      field: 'icarMilkRecordingSchemeType',
      title: 'Какие доения вошли в контроль',
      what: 'Отвечает, измерен суточный удой целиком или досчитан по правилу с одного доения.',
      rows: Object.entries(RECORDING_SCHEME).map(([value, v]) => [value, v.label, v.hint]),
    },
    {
      field: 'icarMilkSamplingSchemeType',
      title: 'Как брали пробу',
      what: 'Меняет жир и белок, а не удой: молоко разных доений различается по составу.',
      rows: Object.entries(SAMPLING_SCHEME).map(([value, v]) => [value, v.label]),
    },
    {
      field: 'icarMilkSamplingMomentType',
      title: 'Когда брали пробу',
      what: 'Утро, вечер или объединённая проба — вместе со схемой задаёт, что попало в анализ.',
      rows: Object.entries(SAMPLING_MOMENT).map(([value, v]) => [value, v.label]),
    },
  ]

  return (
    <>
      <ProductHeader locale={locale} />

      <main className="container-page pb-16">
        <NoteHeader note={note} />

        {/* --------------------------- Случай из жизни -------------------------- */}
        <section className="mt-14 max-w-[75ch]">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            Два хозяйства называют одно число
          </h2>

          <p className="mt-5 text-[16px] leading-relaxed text-ink-700">
            Разговор на выставке или в переписке о покупке нетели выглядит так. Одно хозяйство
            говорит: средний удой по стаду 9&nbsp;640&nbsp;кг за 305&nbsp;дней. Второе называет
            ровно то же самое: 9&nbsp;640&nbsp;кг. Дальше собеседники считают, что сравнили стада,
            и переходят к цене. На самом деле они не сравнили ничего: за двумя одинаковыми числами
            стоят разные способы их получить.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            В первом хозяйстве показания снимал работник службы учёта, приезжавший ежемесячно,
            учитывались все доения контрольных суток, проба бралась объединённая. Во втором
            контроль вёл сам зоотехник хозяйства раз в квартал, записывал одно доение
            контрольного дня, а суточный удой досчитывался по правилу. В привычной записи это{' '}
            <strong className="font-medium">{FIRST}</strong> и{' '}
            <strong className="font-medium">{SECOND}</strong>.
          </p>

          <div className="mt-6 rounded-2xl bg-brand-50 px-6 py-5">
            <p className="text-[16px] leading-relaxed text-ink-700">
              Оба числа могут быть верны. Ни одно из них не подделано. Складывать их в один рейтинг
              всё равно нельзя — и не потому, что второму хозяйству не верят, а потому, что второе
              число никто, кроме второго хозяйства, не видел.
            </p>
          </div>

          <p className="mt-5 text-[16px] leading-relaxed text-ink-700">
            Различий здесь три, и они разной природы. Первое — свидетель: в одном случае
            показания снял человек, которому результат безразличен, в другом — тот, кому
            он не безразличен. Это не обвинение, а разница в доказательности: проверить хозяйскую
            запись задним числом нечем, второго человека у весов не было.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Второе — плотность точек. Лактация не измеряется целиком: её считают по контрольным
            дням, а промежутки между ними досчитывают. При ежемесячном контроле на 305&nbsp;дней
            приходится около десяти замеров и шаг примерно в месяц; при квартальном — три-четыре
            замера и шаг под 90&nbsp;дней. Во втором случае измеренного в лактации меньше,
            а досчитанного больше, и пик лактации может просто не попасть ни в один контрольный
            день.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Третье — что именно записали в контрольный день: все доения суток или одно, из которого
            суточный удой получен по правилу. И отдельно — как брали пробу на анализ; это
            не меняет удой, но меняет жир и белок, а их обычно называют в том же разговоре.
          </p>
        </section>

        {/* ------------------------- Буква и цифра ------------------------------ */}
        <section className="mt-14">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            Что значит буква и что значит цифра
          </h2>

          <p className="mt-5 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Буква отвечает на один вопрос: кто снимал показания. Значений три, и они взяты
            не из отраслевой привычки, а из перечисления стандарта{' '}
            <code className="rounded bg-ink-50 px-1.5 py-0.5 text-[13px]">
              icarMilkRecordingProtocolType
            </code>
            .
          </p>

          <div className="mt-6 space-y-4">
            {Object.entries(RECORDING_PROTOCOL).map(([value, v]) => (
              <div key={value} className="rounded-2xl border border-ink-100 bg-white p-5 sm:p-6">
                <div className="flex flex-wrap items-baseline justify-between gap-3">
                  <span className="text-[16px] font-medium">
                    <span className="stat-value text-forest-600">{v.letter}</span> — {v.label}
                  </span>
                  <code className="text-[12px] text-ink-500">{value}</code>
                </div>
                <p className="mt-3 text-[15px] leading-relaxed text-ink-500">{v.hint}</p>
              </div>
            ))}
          </div>

          <p className="mt-6 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Цифра рядом с буквой — <strong className="font-medium">интервал между
            контрольными днями в неделях</strong>, а не число контролей в год. По руководствам
            ICAR «A4» означает официальный контроль примерно раз в четыре недели (28–33&nbsp;дня),
            «A6» — раз в шесть (38–46&nbsp;дней). В книге записывают привычное хозяйству число
            контролей в год, а интервал считается из него: ежемесячный официальный контроль даёт
            {FIRST}, ежеквартальный хозяйский — {SECOND}. Это и есть то единственное, что
            произносят вслух, когда о методе спрашивают устно.
          </p>

          <h3 className="mt-8 max-w-[75ch] text-[19px] font-medium leading-tight">
            B принимается, но напрямую с A не сравнивается
          </h3>

          <p className="mt-3 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Скажем прямо, потому что обычно этого не говорят. Контроль, который ведёт хозяйство,
            — законный способ учёта, он принят в стандарте, на нём держится вся внутрихозяйственная
            работа: раздача корма по группам, отбор в осеменение, выбраковка. Записи по B —
            настоящие данные, а не черновик.
          </p>

          <p className="mt-4 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Нельзя одного: поставить их в один ряд с записями по&nbsp;A и делать вид, что разницы
            нет. Причина не в честности хозяйства, а в устройстве доказательства: у записи по&nbsp;A
            есть независимый свидетель, у записи по&nbsp;B его нет. Хозяйство может вести учёт
            безупречно — проверить это со стороны всё равно нечем, а рейтинг, смешавший A и&nbsp;B,
            вводит в заблуждение обоих участников: одному приписывает подтверждение, которого
            не было, у другого отнимает преимущество, за которое он платил службе учёта.
          </p>

          <p className="mt-4 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Отсюда правило, по которому книга решает, сравнимы ли два ряда доений, и оно грубое
            намеренно: сравнивать можно записи с одинаковым протоколом. C — «оба поочерёдно» —
            тоже отдельный случай, а не среднее между A и&nbsp;B. А незаполненный метод сопоставим
            только с незаполненным: пока не сказано, кто снимал показания, ставить запись рядом
            с официальной нельзя.
          </p>
        </section>

        {/* ------------------- Почему это не одно поле «A4» --------------------- */}
        <section className="mt-14">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            Почему в книге не одно поле «A4»
          </h2>

          <p className="mt-5 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            «A4» — обозначение из руководства: удобное человеку, неразложимое для машины.
            За ним стоят независимые обстоятельства, и стандарт разводит их по отдельным
            перечислениям именно потому, что они меняются порознь. Служба учёта приезжает
            на ферму с роботами, где считается весь период, а не сутки. Хозяин отбирает пробу
            пропорционально всем доениям, как положено. Обе комбинации встречаются, и склеенная
            строка их не различает.
          </p>

          <p className="mt-4 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Каждое обстоятельство меняет в числе своё. Кто снимал — не само число, а право
            поставить его рядом с чужим. Какие доения вошли — сколько в суточном удое измерено,
            а сколько досчитано. Схема и момент отбора пробы — жир и белок, но не удой.
            Свернув это в строку, мы получили бы поле, которое нельзя ни проверить правилом,
            ни отдать в обмен.
          </p>

          <div className="mt-8 overflow-x-auto">
            <table className="data-table w-full min-w-[720px] text-[14px]">
              <thead>
                <tr>
                  <th className="w-[300px] text-left">Значение стандарта</th>
                  <th className="text-left">Как читается в книге</th>
                </tr>
              </thead>
              <tbody>
                {GROUPS.map((g) => (
                  <Fragment key={g.field}>
                    <tr className="is-group">
                      <td colSpan={2}>
                        <span className="font-medium">{g.title}</span>
                        <span className="ml-2 text-ink-500">·</span>{' '}
                        <code className="text-[12px] text-ink-500">{g.field}</code>
                        <p className="mt-1 text-[13px] leading-snug text-ink-500">{g.what}</p>
                      </td>
                    </tr>
                    {g.rows.map(([value, label, hint]) => (
                      <tr key={value}>
                        <td>
                          <code className="text-[12px]">{value}</code>
                        </td>
                        <td>
                          {label}
                          {hint && <p className="mt-1 text-[13px] text-ink-500">{hint}</p>}
                        </td>
                      </tr>
                    ))}
                  </Fragment>
                ))}
              </tbody>
            </table>
          </div>

          <p className="mt-6 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Значения написаны так, как их пишет ICAR, вплоть до опечатки в одном из них: копия
            схем лежит у нас в дереве, и отдельная проверка следит, чтобы наш список не разошёлся
            с их. Исправить чужую опечатку у себя значило бы сделать выгрузку непринимаемой —
            красиво и бесполезно.
          </p>

          <p className="mt-4 max-w-[75ch] text-[16px] leading-relaxed text-ink-700">
            Привычная запись при этом никуда не девается: {FIRST} и {SECOND} на этой странице
            не набраны буквами, а собраны из полей той же функцией, которая показывает обозначение
            в карточке доения. Хранить его отдельной строкой значило бы завести копию вычислимого;
            такая копия расходится с источником в первый же день правки — и расходится молча,
            потому что выглядит так же убедительно.
          </p>
        </section>

        {/* ----------------------- Что делать хозяйству ------------------------- */}
        <section className="mt-14 max-w-[75ch]">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            Что делать хозяйству
          </h2>

          <h3 className="mt-6 text-[19px] font-medium leading-tight">
            Как понять, какой метод у вас
          </h3>

          <p className="mt-3 text-[16px] leading-relaxed text-ink-700">
            Смотреть надо не в отчёт, а на то, как дело шло на самом деле. Четыре вопроса,
            и ответы на них знает любой, кто был на ферме в контрольный день.
          </p>

          <ol className="mt-4 list-decimal space-y-3 pl-5 text-[16px] leading-relaxed text-ink-700">
            <li>
              <strong className="font-medium">
                Кто физически снимал показания и расписывался?
              </strong>{' '}
              Если это ваш зоотехник или дояр — метод B, что бы ни было написано в отчёте.
              Договор с лабораторией метод не меняет: лаборатория анализирует пробу, а
              не свидетельствует, кто и как её отобрал.
            </li>
            <li>
              <strong className="font-medium">Сколько раз за год это делали?</strong> Это и есть
              цифра. Считать надо фактические контроли, а не запланированные.
            </li>
            <li>
              <strong className="font-medium">
                Что записывали — все доения суток или одно?
              </strong>{' '}
              На роботизированной ферме ответ обычно третий: весь период, а не сутки.
            </li>
            <li>
              <strong className="font-medium">Как и когда отбирали пробу?</strong> Со всех доений
              или через раз, объединённую или утреннюю. Если проб не берут вовсе, эти два поля
              остаются пустыми, а кто снимал и сколько раз — записывается всё равно: поля
              независимы друг от друга.
            </li>
          </ol>

          <h3 className="mt-8 text-[19px] font-medium leading-tight">Что записать в книгу</h3>

          <p className="mt-3 text-[16px] leading-relaxed text-ink-700">
            В карточке контрольного доения есть раздел «Метод контроля продуктивности»: кто
            снимал, сколько контролей в год, какие доения вошли, схема и момент отбора пробы.
            У хозяйства с устоявшимся порядком эти поля одинаковы у всех доений подряд —
            заполняются один раз и меняются тогда, когда меняется сам порядок: сменили службу
            учёта, перешли на роботов, отказались от квартального контроля в пользу ежемесячного.
            Раздел разобран на{' '}
            <Link
              href={`/${locale}/book/milk`}
              className="font-medium text-forest-600 underline underline-offset-4 hover:text-forest-500"
            >
              странице про лактации и контрольные доения
            </Link>
            .
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Работа эта скучная и делается один раз, а нужна дважды. Сегодня — чтобы удой хозяйства
            можно было честно поставить рядом с чужим. Завтра — потому что племенную ценность
            считают по продуктивности дочерей, и данные без метода в такой расчёт войдут в лучшем
            случае с оговоркой (об этом{' '}
            <Link
              href="/ru/razbory/fgias-i-indeks"
              className="font-medium text-forest-600 underline underline-offset-4 hover:text-forest-500"
            >
              разбор про реестр и национальный индекс
            </Link>
            ).
          </p>

          <h3 className="mt-8 text-[19px] font-medium leading-tight">
            Что делать со старыми записями, у которых метод пуст
          </h3>

          <p className="mt-3 text-[16px] leading-relaxed text-ink-700">
            Ничего не делать — и это осознанный ответ, а не отсутствие ответа. У записей,
            внесённых до появления полей, метод пуст, и мы намеренно не проставляем им «{FIRST}»
            или «A4» задним числом: это объявило бы официальным контроль, которого никто
            не проводил. Пустое поле читается как «метод не записан» — так его видит и сама книга:
            без буквы привычное обозначение из полей не собирается вовсе. Такая запись
            сравнивается только с такой же.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Заполнять старые записи стоит в одном случае: когда порядок подтверждён бумагами —
            актами службы учёта, договором, ведомостями контрольных дней. Тогда это не догадка,
            а перенос известного. Проставлять по памяти («у нас всегда так было») не нужно:
            правдоподобное значение хуже пустого, потому что пустое видно, а правдоподобное — нет.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Практический порядок такой: назначить дату, с которой порядок контроля точно известен,
            и заполнять метод от неё вперёд. Всё, что раньше, остаётся без метода. Через год
            у хозяйства будет ряд, пригодный для сравнения снаружи, и ясная граница, до которой
            сравнение не делается.
          </p>

          <h3 className="mt-8 text-[19px] font-medium leading-tight">
            О чём спросить при покупке животного
          </h3>

          <p className="mt-3 text-[16px] leading-relaxed text-ink-700">
            Если в документах на животное или на его мать стоит удой, вопрос ровно один: каким
            методом он получен. Годится и устная форма — «A или B, сколько контролей в год,
            все доения или одно». Ответ «у нас всё по стандарту» ответом не является: B —
            тоже по стандарту.
          </p>

          <p className="mt-4 text-[16px] leading-relaxed text-ink-700">
            Дальше сравнивайте названное число с методом рядом, а не отдельно. Удой по {SECOND}{' '}
            не хуже удоя по {FIRST} — он про другое; но если платить за него как за официальный,
            разница между двумя числами может оказаться разницей методов, а не разницей коров.
            И тот же вопрос стоит задавать себе, когда продаёте вы: хозяйство, которое называет
            метод само, торгуется с позиции сильного.
          </p>
        </section>

        {/* ------------------------------ Пределы ------------------------------- */}
        <section className="mt-14 max-w-[75ch] rounded-2xl border border-ink-100 bg-white p-6 sm:p-8">
          <h2 className="text-[20px] font-medium leading-tight">Пределы, названные до вопроса</h2>

          <div className="mt-5 space-y-4 text-[15px] leading-relaxed text-ink-500">
            <p>
              <strong className="font-medium text-ink-700">
                Книга не проверяет, что метод назван верно.
              </strong>{' '}
              Поля заполняет человек, и написать A там, где было B, ничто не мешает. Проверить это
              может только тот, кто ведёт учёт, — организация, а не программа. Наша работа
              в другом: чтобы у числа было место, где сказано, откуда оно, и чтобы это место
              нельзя было заполнить умолчанием.
            </p>
            <p>
              <strong className="font-medium text-ink-700">
                Интервал между контролями пока не проверяется.
              </strong>{' '}
              Правило ловит полный разрыв в ряду доений, а не растянутый интервал: заявленные
              двенадцать контролей в год при фактических четырёх книга сегодня не поймает. Это
              названный пробел, он стоит в{' '}
              <Link
                href={`/${locale}/icar/gaps#milk-recording`}
                className="underline underline-offset-4 hover:text-forest-500"
              >
                списке несделанного
              </Link>
              , а не в планах на потом.
            </p>
            <p>
              <strong className="font-medium text-ink-700">Поля необязательны.</strong> Ни одно
              из них не требуется для сохранения доения, и это выбор, а не недоделка: обязательное
              поле в старой книге заставляет придумывать значение, а придуманное значение
              неотличимо от настоящего.
            </p>
          </div>
        </section>

        {/* ------------------------ Чего это не доказывает ---------------------- */}
        <section className="mt-14 max-w-[75ch]">
          <h2 className="text-[24px] font-medium leading-tight sm:text-[28px]">
            Чего этот разбор не доказывает
          </h2>

          <div className="mt-5 space-y-4 text-[16px] leading-relaxed text-ink-700">
            <p>
              <strong className="font-medium">Что B хуже A по существу.</strong> Речь не о качестве
              хозяйства и не о добросовестности зоотехника: аккуратный хозяйский учёт бывает
              точнее небрежного официального. Речь об одном — есть ли у записи независимый
              свидетель. Это свойство процедуры, а не людей, и меняется оно приглашением службы
              учёта, а не старанием.
            </p>
            <p>
              <strong className="font-medium">
                Что записанный метод делает лактацию точной.
              </strong>{' '}
              Он делает её сопоставимой. Точность зависит от другого: от числа контрольных дней,
              от того, попал ли в них пик, от работы лаборатории. Метод — не улучшение числа,
              а сведения о том, с чем это число разрешено ставить рядом.
            </p>
            <p>
              <strong className="font-medium">
                Что метод у старых записей можно восстановить.
              </strong>{' '}
              Нельзя. Из самих чисел он не выводится: ряд по A и ряд по B выглядят одинаково,
              на то и весь разговор. Восстанавливается он только документами хозяйства, и если
              их нет, запись остаётся без метода навсегда. Мы не станем достраивать это ни
              умолчанием, ни правдоподобной догадкой.
            </p>
          </div>
        </section>

        <NoteSources note={note} />
        <NoteNeighbours notes={NOTES} current={SLUG} />
      </main>

      <ProductFooter lang={locale} />
    </>
  )
}
