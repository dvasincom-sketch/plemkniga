import { redirect } from 'next/navigation'

/**
 * Адрес без языка.
 *
 * На него ведёт подвал книги: ставить там `/ru/org` значило бы решать
 * за читателя язык на другом домене, а витрина определяет его лучше —
 * по браузеру. Перенаправление ведёт на русскую версию, полную;
 * с неё переключатель уводит на любую другую.
 */
export default function RedirectOrg() {
  redirect('/ru/org')
}
