import type { Metadata } from 'next'
import React from 'react'

/**
 * Кабинет хозяйства закрыт от поисковых систем.
 *
 * Не из осторожности: страницы за входом робот и так не откроет.
 * Но адреса из кабинета попадают в письма и в переписку, а оттуда —
 * в индекс, и в выдаче появляется строка «Стадо — Племенная книга»
 * с адресом, который посторонний открыть не может. Для человека
 * это выглядит поломкой сайта, а для нас — приглашением стучаться
 * в закрытую дверь.
 *
 * Запрет ставится раскладкой, а не каждой страницей: страниц
 * в кабинете два десятка, и забыть пометку на одной проще, чем
 * не забыть на всех.
 */
export const metadata: Metadata = {
  robots: { index: false, follow: false },
}

export default function AccountLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
