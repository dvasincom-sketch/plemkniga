/**
 * Сообщение вместо стека, когда сервер работает со старой схемой.
 *
 * Показывается только в разработке и только по одной причине: Payload держит
 * копию конфигурации с момента запуска процесса, и всё, что добавили
 * в коллекции позже, для него не существует. Человеку в этот момент нужна
 * не трассировка стека, а одна строка — «перезапустите сервер».
 */
export function StaleSchemaNotice({ what }: { what: string }) {
  return (
    <div className="card">
      <h2 className="panel-heading">Раздел не поднялся</h2>
      <p className="mb-4 max-w-[70ch] text-[15px] leading-relaxed text-ink-700">
        Сервер работает со схемой, построенной при его запуске, — {what} в ней ещё нет. Данные
        в порядке, дело только в запущенном процессе.
      </p>
      <p className="max-w-[70ch] text-[15px] leading-relaxed text-ink-700">
        Перезапустите сервер разработки: <code className="rounded bg-canvas px-1.5 py-0.5">npm run dev</code>.
        Горячая перезагрузка здесь не помогает — она пересобирает страницы, а не конфигурацию
        Payload. На боевом сервере такого не бывает: там каждая выкладка — новый процесс.
      </p>
    </div>
  )
}
